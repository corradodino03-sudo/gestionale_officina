# ============================================================
# .env.example - Template configurazione ambiente
# Progetto: Garage Manager (Gestionale Officina)
#
# ISTRUZIONI PER L'UTENTE:
# 1. Copiare questo file in .env
# 2. Modificare i valori secondo le proprie esigenze
# 3. Non condividere il file .env (contiene credenziali)
# ============================================================

# ------------------------------------------------------------
# CONFIGURAZIONE DATABASE POSTGRESQL
# ------------------------------------------------------------

# Nome utente database (default: garage_user)
POSTGRES_USER=garage_user

# Password database (CAMBIARE IN PRODUZIONE!)
POSTGRES_PASSWORD=changeme

# Nome database (default: garage_db)
POSTGRES_DB=garage_db

# Porta PostgreSQL (default: 5432)
POSTGRES_PORT=5432

# URL connessione database (formato async per SQLAlchemy 2.0)
# Formato: postgresql+asyncpg://user:password@host:port/database
# Nota: il valore @db:5432 fa riferimento al servizio docker-compose
DATABASE_URL=postgresql+asyncpg://garage_user:changeme@db:5432/garage_db


# ------------------------------------------------------------
# CONFIGURAZIONE APPLICAZIONE
# ------------------------------------------------------------

# Ambiente di esecuzione (development | production)
APP_ENV=development

# Modalità debug (true | false)
# In produzione impostare sempre a false
DEBUG=true

# Chiave segreta per sessioni/token (CAMBIARE IN PRODUZIONE!)
# Generare con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme-in-production-replace-with-secure-key

# Porta backend (default: 8000)
BACKEND_PORT=8000

# Titolo applicazione
APP_NAME=Garage Manager

# Versione applicazione
APP_VERSION=1.0.0


# ------------------------------------------------------------
# CONFIGURAZIONE CORS (Cross-Origin Resource Sharing)
# ------------------------------------------------------------

# Origini CORS permesse (separare con virgola)
# Per sviluppo locale: http://localhost:3000,http://127.0.0.1:3000
# Per accesso LAN: http://192.168.x.x:3000
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


# ------------------------------------------------------------
# CONFIGURAZIONE LOGGING
# ------------------------------------------------------------

# Livello logging (DEBUG | INFO | WARNING | ERROR | CRITICAL)
LOG_LEVEL=INFO


# ------------------------------------------------------------
# CONFIGURAZIONE FATTURAZIONE (Opzionale - MVP)
# ------------------------------------------------------------

# Ragione sociale officina
INVOICE_COMPANY_NAME=Tua Officina S.r.l.

# Partita IVA
INVOICE_TAX_ID=01234567890

# Indirizzo
INVOICE_ADDRESS=Via Example, 1 - 00100 Roma (RM)

# Telefono
INVOICE_PHONE=+39 06 1234567

# Email
INVOICE_EMAIL=info@tuOfficina.it


# ------------------------------------------------------------
# CONFIGURAZIONE BACKUP (Opzionale)
# ------------------------------------------------------------

# Abilita backup automatico (true | false)
BACKUP_ENABLED=false

# Percorso cartella backup (default: ./backups)
BACKUP_PATH=./backups

# Giorni retention backup (default: 30)
BACKUP_RETENTION_DAYS=30
